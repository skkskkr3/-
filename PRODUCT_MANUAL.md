# 知序｜学习通学习助手 MVP 产品说明书

版本：0.4 本机测试版（2026-09-24）

当前进度：已在真实账号完成双平台只读导入验证。当前测试数据可读取学习通课程与多页作业状态，以及教务系统课表和成绩；打开看板会按间隔自动刷新。平台官网结构变化后仍可能需要更新适配器，页面会显示明确的同步状态，不把空白误报为成功。

## 1. 产品做什么

知序是面向学生的课程任务工作台。学生可选学习通或湖北第二师范学院教务系统官网登录，在本机弹出的官网页面自行完成身份验证。登录一种服务后，可在看板中关联另一种。学习通用于课程任务与作业，教务系统只读查询本学期课表、本学期与往年成绩。学生可以补录任务、上传老师资料，并主动启动任务整理或 AI 草稿流程。任何草稿都需要学生查看、修改和批准。

当前版本的重点是让你能亲自检查完整交互。它**不会自动向学习通提交作业**，也没有部署成可供公众访问的网站。

## 2. 现在能用的功能

| 功能 | 当前行为 |
| --- | --- |
| 学习通账号登录 | 在独立的 Microsoft Edge 窗口打开学习通官网，由学生自行登录；课程、作业、附件按学习通用户 ID 隔离；知序没有独立密码 |
| 教务系统账号登录 | 在独立的 Microsoft Edge 窗口打开 `jwxt.hue.edu.cn` 官网，学生自行登录；可单独建立学习空间，或在已登录的空间关联；不接收教务密码 |
| 课表与成绩 | 关联后自动只读读取本学期课表和已发布的本学期/往年成绩，并保存在本机数据库；失败时保留上次成功结果且不显示猜测数据 |
| 看板 | 显示课程数、学习通待完成、本周截止、任务总数、近期任务和动态，并集中显示两个平台的连接状态 |
| 学习通会话 | 在本机打开学习通网页，由学生本人登录；登录会话保存在各自的本地浏览器资料目录 |
| 本学期课程与作业同步 | 以教务课表作为本学期课程白名单，只从网页版“我的课程”“我的作业”录入匹配课程及其任务；用课程、班级和作业 ID 稳定去重；同步未提交、待批阅、完成及互评状态 |
| 课程详情 | 点击左侧本学期课程可查看教师、教室、本周课次、待办数量、最近截止任务和数据来源；支持“全部 / 有待办 / 今天”筛选 |
| 原始资料读取 | 对拥有学习通原始链接的任务，尝试读取任务页面文字和直接链接的 PDF、DOCX、文本等资料 |
| 手动补录与修正 | 当学校页面不兼容自动同步时，可手工添加任务；同步结果有误时可编辑课程、标题、期限和老师要求 |
| 附件 | 上传 PDF、DOCX、TXT、MD、CSV、PY、JSON、PNG、JPG，每个文件最多 10 MB；文本类文件会提取文字 |
| 截止提醒 | 登录看板时按 7 天、3 天、24 小时、逾期阈值生成站内动态，并去重 |
| 开始执行 | 普通作业、实验和报告可由学生主动启动；考试只管理时间与资料 |
| AI 草稿 | 配置 OpenAI API Key 后，依据任务说明与可提取资料生成 Markdown 审核草稿；缺少依据之处要求标记“待核实” |
| 无 AI Key 时 | 仍能生成任务清单和执行计划，但不会声称已完成可交付作业 |
| 人工审核 | 编辑、重新生成、批准版本；编辑或重新生成会使旧批准失效 |
| 导出 | 将当前草稿导出为 Markdown 文件，供学生检查与手动提交 |

## 3. 五分钟启动

运行环境：Windows 10/11、Python 3.10+、已安装 Microsoft Edge；首次安装 Python 依赖时需要联网。当前程序只监听 `127.0.0.1:8000`，仅本机可访问。

1. 打开 PowerShell，进入本项目目录：

   ```powershell
   Set-Location 'C:\Users\31480\Desktop\学习通作业助手'
   ```

2. 在桌面项目文件夹双击 `启动知序.cmd`，保持弹出的命令窗口开启。或者运行：

   ```powershell
   powershell -ExecutionPolicy Bypass -File .\run.ps1
   ```

3. 等待看到 `Uvicorn running on http://127.0.0.1:8000`，然后浏览器打开 <http://127.0.0.1:8000>。

4. 选择“使用学习通登录”或“使用教务系统登录”，在弹出的官网窗口完成登录。成功后可在对应页面关联另一种服务。

首次运行会安装 Python 包，时间取决于网络。以后启动仍会检查依赖。关闭 PowerShell 中正在运行的服务后，网页就无法使用。修改程序代码后也需要关闭旧服务，再双击 `启动知序.cmd` 重启；不要同时运行两个占用 8000 端口的服务。

### 看板上的第一步

如果你像当前截图一样已进入看板，但“课程数量”为 0、“尚未关联学习通”，说明当前学习空间还没有学习通课程数据。点击右侧“关联学习通”，在弹出的 Edge 官网窗口完成首次登录；知序会自动开始读取课程和作业，并在看板更新状态。若读取后仍为 0，请看状态文字，不要把空白看板当作导入成功。用教务系统登录过的用户无需每天重新登录，只需打开看板等待自动刷新；官网会话过期时会显示“重新登录教务系统”。两个平台是独立账号，第一次关联时仍需各自到官网登录一次。

### 可选：启用 AI 草稿

在运行 `run.ps1` 的同一个 PowerShell 窗口中，先设置环境变量：

```powershell
$env:OPENAI_API_KEY = '你的 OpenAI API Key'
$env:OPENAI_MODEL = 'gpt-6-luna'
powershell -ExecutionPolicy Bypass -File .\run.ps1
```

API Key 需要自行获取并具有相应模型的 API 使用权限；API 调用可能产生费用。密钥只在服务端环境变量中使用，不要填在网页任务说明中，也不要提交到代码仓库。配置后，点击“开始执行”会将任务说明和可提取的附件文字发送给模型。OpenAI 官方文档建议通过 Responses API 发起文本生成请求；本项目按该接口实现。[官方文本生成指南](https://developers.openai.com/api/docs/guides/text)

## 4. 学生使用流程

### 4.1 使用学习通登录

在首页点击“使用学习通登录”，或在看板点击“关联学习通”。程序会在**运行服务的这台 Windows 电脑上**弹出独立的 Microsoft Edge 窗口，跳转到学习通网页。你在该窗口自行输入学习通账号密码或扫码登录，知序网页不接收密码。请先关联教务系统并成功读取本学期课表；知序会把课表课程作为白名单，只录入本学期匹配的学习通课程和作业，不再导入历年课程。进入看板后会自动尝试同步，“同步课程任务”按钮用于手动立即重试。

学习通入口为 `https://i.mooc.chaoxing.com/space/index` 的“我学的课”页面。虽然使用电脑上已安装的 Edge 程序，知序仍使用独立的浏览器资料目录，**不会自动继承你平时 Edge 中保存的密码或登录状态**，所以第一次仍需在弹出的窗口自行登录。不要关闭弹窗，等待知序确认登录；确认后弹窗会自动关闭。若学习通会话过期，退出知序后重新使用学习通登录。

本功能依赖网页版页面结构。课程或作业没有抓到时，可先从学习通复制任务链接，使用“添加任务”补录，再在详情页点击“读取老师要求与资料”。若仍无法读取，可直接粘贴要求并上传附件。页面兼容性需要用你所在学校的账号进一步验证。

### 4.1a 教务系统课表与成绩

在首页可先选择“使用教务系统登录”，也可先登录学习通，再打开侧边栏“课表与成绩”关联教务账号。程序会在独立 Edge 窗口打开学校官网 `https://jwxt.hue.edu.cn/jsxsd/`，由学生自行输入密码/验证码。关联后自动只读访问官网的个人首页、学期理论课表与成绩查询页；“读取最新教务数据”用于手动立即重试。成功识别的数据会显示在课表和成绩面板中，今天的课程也会出现在看板；若实际页面与当前解析器不同，会提示“尚未适配”，不显示猜测数据。课表、成绩均以教务官网为准。

成绩面板按学期分区，可切换“全部学期”或单个学期，并可只看未通过课程。顶部只展示教务系统对应的四项指标：课程数量、所修总学分、平均学分绩点、平均成绩。课程明细保留正常考试、补考、重修和不及格记录，原始考试性质不被覆盖；同一课程用于汇总时采用成绩单中的最高成绩，其他考试记录仅作明细展示，不重复计入课程数、学分和平均成绩。五级制课程优先使用官网课程绩点折算平均成绩。正式成绩、重修标记和学分认定以学校成绩单为准。

两种账号若已分别建立不同学习空间，本版不会擅自合并数据，请先在要保留的学习空间登录，再关联尚未绑定的服务。它不会自动复用 Edge 已登录状态。学校官方教务处公布的学生端入口为 `https://jwxt.hue.edu.cn/jsxsd`，见[学校通知](https://jwc.hue.edu.cn/2025/0923/c9519a190949/pagem.htm)。

### 4.2 查看期限与新作业

看板先显示仍需学生处理的任务，再按学习通提供的发布时间从新到旧排列。若官网没有提供发布时间，则明确标为“同步时间”并以首次同步时间兜底，不伪造发布时间。7 天内到期的任务计入“本周截止”；新作业、完成状态或截止时间变化会进入“最近动态”。提醒只在应用运行并打开看板时出现；当前没有手机推送、邮件或关机后后台监控。

### 4.3 读取要求和上传资料

进入任务详情，核对老师原始要求。学习通同步任务通常会有原始链接。点击“读取老师要求与资料”会尝试读取页面文字并下载可直接识别的文件链接。若课程、标题或期限识别有误，点击“编辑任务信息”修正。图片会保存，但本版没有 OCR，图片里的文字不会参与 AI 草稿。扫描版 PDF 也可能无法提取文字。对未自动识别的要求，请手工补充到任务描述或上传文本版文件。

### 4.4 启动任务与审核

点击“开始执行”。有 API Key 时，系统生成一个可编辑的 Markdown 草稿；没有 API Key 时，系统只生成执行计划。生成结果会显示在任务详情中。学生可以修改内容并保存新版本，或重新生成。只有状态为“待审核”的草稿可点击“批准此版本”。批准不等于提交学习通，仍需你导出并自行检查和提交。

实验数据、程序运行截图、引用来源与个人经历必须由学生核实。当前版本不会自动创建隔离实验环境，也不会运行代码或自动形成真实实验结果。

## 5. 数据与隐私

- 应用数据库：`data/study.db`。
- 学生上传的原始文件：`data/files/<知序用户 ID>/<任务 ID>/`。
- 每位学生的学习通浏览器资料：`data/browser_profiles/login_<随机标识>/`。这里包含浏览器资料和用于后台只读同步的 `storage_state.json` 登录状态；持有这些文件的人可能能访问对应账号，因此不要共享、上传或通过聊天发送此目录。
- 教务系统浏览器资料：`data/browser_profiles/jwxt_<随机标识>/`，同样包含敏感登录态。教务学生编号不以明文写入数据库，仅保存本地密钥计算的摘要；`data/identity.key` 与浏览器资料一样需要保密。
- 教务课表与成绩以 JSON 形式保存在本机 `study.db`，服务重启后仍可显示；这些数据不会进入 AI 草稿请求。数据库和备份属于敏感个人数据，应只保存在本人可信电脑上。
- 浏览器安装文件：`.browsers/`。该目录和 `data/` 已加入 `.gitignore`。
- 知序不接收或保存学习通/教务系统明文密码。登录动作发生在各自官网；本地登录 Cookie 为 HttpOnly，默认有效期 14 天。浏览器会话文件仍属于敏感信息，本机使用者需自行保护电脑与备份。
- 当前版本服务仅监听本机地址。它尚未具备公开互联网部署所需的完整安全措施，例如 HTTPS、跨站请求防护、账号找回、限流、密钥托管、后台任务持久化和运营监控。

备份时，在停止服务后复制 `data/` 目录到你信任的位置。该目录包含课程、作业、上传文件和学习通登录态，需要妥善保管。

## 6. 当前限制与下一版重点

1. **双平台同步仍依赖官网页面结构。** 当前真实账号已完成端到端只读验证，但学校或学习通改版后可能需要重新适配；解析失败时不会伪造数据。
2. **真实提交未实现。** 当前“批准”只代表学生确认本地草稿。未来会加入提交前检查、确认绑定版本、回执核验。
3. **任务执行仅到资料整理和 AI 草稿。** 自动搭建实验环境、运行代码、生成可验证实验报告属于下一阶段。
4. **通知仅在站内。** 若要在电脑关闭网页时仍及时提醒，需要独立后台进程或常开服务；手机推送也尚未接入。
5. **AI 结果不保证可直接交付。** 草稿可能遗漏老师要求或包含错误，应逐项核对；没有实际实验记录时不会有可信的实验结论。
6. **多用户目前只用于本机测试。** 官网登录窗口会出现在运行服务的电脑上。当前没有生产级加密存储、审计、撤销连接和自助数据删除，不能公开给其他学生使用；ToC 上线前必须完成安全设计和授权审查，不能承诺绝对安全。

## 7. 给开发者的结构说明

- `app.py`：FastAPI 接口、SQLite 数据模型、账号、任务、文件、学习通浏览器适配和 AI 草稿。
- `static/index.html`、`static/style.css`、`static/app.js`：无构建步骤的前端界面。
- `run.ps1`：创建虚拟环境、安装依赖和浏览器、启动本地服务。
- `requirements.txt`：Python 依赖。
- `ARCHITECTURE.md`：长期架构与迭代路线。

当前选择 Python + FastAPI + SQLite + 原生前端，是为了让编程初学者能启动、观察和修改一套完整流程。等学习通适配器经真实账号验证，且页面交互稳定后，再考虑更复杂的前端框架和云端基础设施。FastAPI 官方文档提醒，耗时任务需要独立执行机制；下一版会把同步和 AI 生成改为可恢复的后台任务。[FastAPI 后台任务说明](https://fastapi.tiangolo.com/tutorial/background-tasks/)

## 8. 建议的测试清单

1. 分别用两个学习通账号登录，各自添加任务，确认彼此看不到对方的数据。不要在同一个登录窗口直接切换账号；先退出知序，再开始新登录。
2. 首次关联后不点击手动读取，观察看板是否自动出现读取状态、课程和任务；如果官网会话已过期，观察“重新登录”入口。
3. 在一个账号里添加带截止时间的任务，观察看板计数和动态提醒。
4. 上传一份小的 TXT、PDF 或 DOCX，检查附件列表和下载。
5. 未配置 AI Key 时点击“开始执行”，确认显示“执行计划”而非已完成成果。
6. 配置 API Key 后用一份不含敏感信息的任务说明试生成草稿，核对其来源与“待核实”提示。
7. 修改草稿后保存，确认版本号增加；批准后再修改，确认需要重新审核。
8. 若自动读取抓取不到，记录学校入口网址、页面标题、读取状态文字与页面结构的脱敏截图，作为下一轮适配依据；不要发送 Cookie 或密码。

## 9. 后续路线

| 阶段 | 目标 |
| --- | --- |
| 0.2 | 用真实学生账号验证课程、作业、期限与附件读取；为不同学校页面建立适配器测试样本 |
| 0.3 | 后台定时同步、桌面/手机提醒、失败重试与运行日志 |
| 0.4 | 针对一种实验类型建立受控执行环境、真实运行记录和交付质量报告 |
| 0.5 | 版本绑定的提交前检查、人工批准后提交、回执核验与成绩回收 |
| 公测前 | HTTPS、每用户安全连接、限流、CSRF 防护、隐私说明、数据删除和备份恢复 |
